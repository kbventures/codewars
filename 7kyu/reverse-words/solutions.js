function reverseWords(str) {
    // Go for it
    let splitStr = str.split(' ')

    // console.log(splitStr)

    // let tmp = splitStr.map(e => {
    //     console.log(e.split(''))
    //     e.split('').reverse().join('');
    // })

    // console.log(tmp)
    console.log(splitStr)
}






reverseWords("This  is  an  example!")

// var stringArray = str.split(/(\s+)/);
