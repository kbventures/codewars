// PREP 
// Parameter is an integer
// Returns "Even" if even and "Odd" if odd
// 
function even_or_odd(number) {
    // Pseusocode number module 2 === 0 return 'Even' else 'Odd'
    return number % 2 === 0 ? 'Even' : 'Odd'
}

  // example even_or_odd(2) returns "Even"